<div class = "show">
<h1>All the Shortlies</h1>

<table style="width: 100%">
  <tr>
    <th>Longly</th>
    <th>Shortly</th>
  </tr>


    <% @sites.each do |s| %>
     <tr>
      <td><%= s.name[0,40] %>...</td>
      <td>www.ortly.herokuapp.com/<%= s.id %></td>
     </tr>
    <% end %>

</table>
</div>
